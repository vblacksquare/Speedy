[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "Jsonified"
version = "0.0.1"
description = "Jsonified"
authors = [{ name = "Denis Minka", email = "minka.denis@gmail.com" }]
dependencies = []
